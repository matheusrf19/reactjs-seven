import React from 'react'

export default props =>
    <div>
        
        <div class="Linha">
            <h3>Algumas Habilidades</h3>
            {props.children}
        </div>
    </div>